apiVersion: v1
kind: Namespace
metadata:
  name: {{ company }}-{{ domain }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ company }}-{{ domain }}-consumers-secrets
  namespace: {{ company }}-{{ domain }}
type: Opaque
stringData:
  Auth__Authority: "___AUTH_AUTHORITY___"
  Auth__Audience: "___AUTH_AUDIENCE___"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ company }}-{{ domain }}-consumers
  namespace: {{ company }}-{{ domain }}
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: {{ company }}-{{ domain }}-consumers
  template:
    metadata:
      labels:
        app: {{ company }}-{{ domain }}-consumers
    spec:
      containers:
      - name: {{ company }}-{{ domain }}-consumers
        image: "___ACR_NAME___.azurecr.io/{{ company }}-{{ domain }}-consumers:___IMAGE_TAG___"
        ports:
        - containerPort: 8080
        envFrom:
        - secretRef:
            name: {{ company }}-{{ domain }}-consumers-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: {{ company }}-{{ domain }}-consumers
  namespace: {{ company }}-{{ domain }}
spec:
  selector:
    app: {{ company }}-{{ domain }}-consumers
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
