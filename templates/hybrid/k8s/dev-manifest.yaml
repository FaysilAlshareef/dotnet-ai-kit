apiVersion: v1
kind: Namespace
metadata:
  name: {{ company }}-{{ domain }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ company }}-{{ domain }}-commands-secrets
  namespace: {{ company }}-{{ domain }}
type: Opaque
stringData:
  ConnectionStrings__DefaultConnection: "___CONNECTION_STRING___"
  ServiceBus__ConnectionString: "___SERVICEBUS_CONNECTION___"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ company }}-{{ domain }}-commands
  namespace: {{ company }}-{{ domain }}
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: {{ company }}-{{ domain }}-commands
  template:
    metadata:
      labels:
        app: {{ company }}-{{ domain }}-commands
    spec:
      containers:
      - name: {{ company }}-{{ domain }}-commands
        image: "___ACR_NAME___.azurecr.io/{{ company }}-{{ domain }}-commands:___IMAGE_TAG___"
        ports:
        - containerPort: 8080
        envFrom:
        - secretRef:
            name: {{ company }}-{{ domain }}-commands-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: {{ company }}-{{ domain }}-commands
  namespace: {{ company }}-{{ domain }}
spec:
  selector:
    app: {{ company }}-{{ domain }}-commands
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
