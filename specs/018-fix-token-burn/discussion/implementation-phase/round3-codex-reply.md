## Round-2 fixes verified

- **pre-bash/pre-commit Python probe**: The new candidate loop requires `python3`, `python`, or `py` to both exist and successfully run `-c "import sys"` before stdin JSON parsing uses it (`hooks/pre-bash-guard.sh:20`, `hooks/pre-bash-guard.sh:22`, `hooks/pre-commit-lint.sh:20`, `hooks/pre-commit-lint.sh:22`). That is the right shape for Linux/macOS and should reject a Windows app-execution alias that cannot actually run `-c`. I could not execute the hook here because this machine has no `python`, `python3`, or `py` on PATH.
- **copy_skills per-skill skip warning**: The warning includes the skill-relative path and the missing-key error (`src/dotnet_ai_kit/copier.py:436`, `src/dotnet_ai_kit/copier.py:444`), so a maintainer can identify the skipped source skill. I do not think the current tests prove the user-visible warning contract; see Test gaps.
- **upgrade pre-check no-manifest behavior**: The new drift check only runs when `read_manifest()` returns a manifest (`src/dotnet_ai_kit/cli.py:1185`, `src/dotnet_ai_kit/cli.py:1186`), so a legacy install with no manifest proceeds without a false-positive block. It sits after the up-to-date version guard (`src/dotnet_ai_kit/cli.py:1176`) and before the early dry-run return (`src/dotnet_ai_kit/cli.py:1220`).
- **mcp-state.yml move**: Runtime code now writes `.dotnet-ai-kit/mcp-state.yml`, not `config.yml` (`src/dotnet_ai_kit/cli.py:305`, `src/dotnet_ai_kit/cli.py:308`, `src/dotnet_ai_kit/cli.py:317`, `src/dotnet_ai_kit/cli.py:342`). I found stale docs/spec references to config.yml, but no remaining runtime reader/writer of `config.yml :: mcp.*`.
- **multi-repo rule path tightening**: `rules/multi-repo.md` is now scoped to briefs/service-map/events/protos, not all `src/**/*.cs` (`rules/multi-repo.md:3`).
- **smoke PATH fix**: The MCP fallback smoke test now prepends a shim directory and preserves the rest of PATH (`tests/smoke/test_mcp_fallback.py:26`, `tests/smoke/test_mcp_fallback.py:40`).

## New findings

1. **CRITICAL - `/dai.upgrade` still is not atomic on the CLI path.** The spec makes atomic rollback a hard requirement (`specs/018-fix-token-burn/spec.md:189`, `specs/018-fix-token-burn/spec.md:228`), but `cli.py::upgrade` still executes the legacy flow directly: it makes `.bak` directory copies (`src/dotnet_ai_kit/cli.py:1319`, `src/dotnet_ai_kit/cli.py:1326`, `src/dotnet_ai_kit/cli.py:1335`), overwrites commands/rules/skills/agents in place (`src/dotnet_ai_kit/cli.py:1341`, `src/dotnet_ai_kit/cli.py:1354`, `src/dotnet_ai_kit/cli.py:1358`, `src/dotnet_ai_kit/cli.py:1365`), catches later hook and linked-repo failures as warnings (`src/dotnet_ai_kit/cli.py:1401`, `src/dotnet_ai_kit/cli.py:1403`, `src/dotnet_ai_kit/cli.py:1423`), writes the version (`src/dotnet_ai_kit/cli.py:1430`), and refreshes the manifest after the fact (`src/dotnet_ai_kit/cli.py:1448`, `src/dotnet_ai_kit/cli.py:1451`). The real transactional function exists (`src/dotnet_ai_kit/upgrade.py:106`), but the CLI does not route through it. T043a being `[~]` is honest, but the spec says this is a release gate, not a follow-up.

2. **HIGH - `copy_skills(..., detected_paths=None)` deploys literal `${detected_paths.*}` tokens.** `init --type command` creates a `DetectedProject` with project type but no `detected_paths` (`src/dotnet_ai_kit/cli.py:548`, `src/dotnet_ai_kit/cli.py:552`), then passes that `None` through to `copy_skills` (`src/dotnet_ai_kit/cli.py:627`, `src/dotnet_ai_kit/cli.py:633`, `src/dotnet_ai_kit/cli.py:675`, `src/dotnet_ai_kit/cli.py:679`). `copy_skills` only resolves or rejects tokens when the mapping is truthy (`src/dotnet_ai_kit/copier.py:433`), so token-bearing skills are copied unchanged. The test suite explicitly preserves that broken behavior (`tests/test_copier_skills.py:81`, `tests/test_copier_skills.py:89`, `tests/test_copier_skills.py:94`). This violates the no-literal-token/fail-closed contract (`specs/018-fix-token-burn/spec.md:155`, `specs/018-fix-token-burn/spec.md:191`).

3. **HIGH - the round-2 "skip unresolvable skill" behavior contradicts FR-033.** Missing token keys are now swallowed per skill (`src/dotnet_ai_kit/copier.py:438`, `src/dotnet_ai_kit/copier.py:444`, `src/dotnet_ai_kit/copier.py:445`), and the replacement test asserts skip-not-abort (`tests/test_copier_skills.py:96`, `tests/test_copier_skills.py:101`, `tests/test_copier_skills.py:124`, `tests/test_copier_skills.py:129`). But FR-033 says missing `${detected_paths.*}` keys must produce a clear warning/error and deployment must abort (`specs/018-fix-token-burn/spec.md:191`). If the intended product behavior is "skip irrelevant skills," the spec and SC-016 need an explicit amendment; otherwise this implementation is wrong.

4. **HIGH - linked-repo auto-init still produces unresolved skill tokens.** For missing secondary repo `project.yml`, `_write_basic_project_yml()` writes only mode/type/confidence, not detected paths (`src/dotnet_ai_kit/copier.py:826`, `src/dotnet_ai_kit/copier.py:831`, `src/dotnet_ai_kit/copier.py:837`). The later linked-repo deploy reads that file, gets `detected_paths=None`, and passes it into `copy_skills` (`src/dotnet_ai_kit/copier.py:1051`, `src/dotnet_ai_kit/copier.py:1073`, `src/dotnet_ai_kit/copier.py:1075`, `src/dotnet_ai_kit/copier.py:1121`, `src/dotnet_ai_kit/copier.py:1125`). That directly violates the linked-repo edge-case promise that missing project.yml still fails closed for path tokens (`specs/018-fix-token-burn/spec.md:130`).

5. **MEDIUM - deployed skill descriptions still contain odd multiline single-quoted scalars.** Example source frontmatter is `description: 'Use when ...` followed by a blank line and an indented closing quote (`skills/api/caching-strategies/SKILL.md:3`, `skills/api/caching-strategies/SKILL.md:5`, `skills/microservice/command/aggregate-design/SKILL.md:3`, `skills/microservice/command/aggregate-design/SKILL.md:5`). `test_top_level_description_is_use_when_trigger` strips the parsed value before checking it (`tests/test_skill_frontmatter.py:66`), so it would not catch trailing newline/space damage. I could not run PyYAML locally because no Python launcher is available, but the source format is still unnecessarily risky for Claude Code's skill router.

6. **LOW - stale MCP state docs now contradict the implementation.** `_record_mcp_state()` writes `mcp-state.yml` (`src/dotnet_ai_kit/cli.py:308`, `src/dotnet_ai_kit/cli.py:317`), but the changelog says the outcome is persisted to `.dotnet-ai-kit/config.yml` (`CHANGELOG.md:40`) and quickstart says init records state in `.dotnet-ai-kit/config.yml` (`specs/018-fix-token-burn/quickstart.md:58`). The spec still requires config.yml for FR-019/SC-014 (`specs/018-fix-token-burn/spec.md:170`, `specs/018-fix-token-burn/spec.md:229`), so either update the spec/tasks or move the state into the pydantic schema.

7. **LOW - the path-token error message points users at the wrong directory.** The `DeploymentError` text says `.dotnet-ai/project.yml` (`src/dotnet_ai_kit/copier.py:389`), but this repo stores project state under `.dotnet-ai-kit/project.yml`.

## Test gaps

1. **HIGH - the v2.1.85 dynamic-hook test monkeypatches the wrong symbol.** `copier.py` imports `check_claude_code_version` directly (`src/dotnet_ai_kit/copier.py:21`) and calls that local name (`src/dotnet_ai_kit/copier.py:712`). The test monkeypatches `dotnet_ai_kit.version_check.check_claude_code_version` (`tests/test_copier_hooks.py:279`, `tests/test_copier_hooks.py:281`), which does not affect the already-imported function in `copier.py`. On a CI runner without Claude Code >= 2.1.85, `test_handler_if_filters_used_when_v2185` will exercise the legacy branch and fail or pass only because the host happens to have a particular `claude` binary.

2. **MEDIUM - no test asserts the per-skill skip warning or skipped count under partial token coverage.** The new skip test checks `count == 1` and absence of one file (`tests/test_copier_skills.py:121`, `tests/test_copier_skills.py:124`, `tests/test_copier_skills.py:129`), but it does not use `caplog` to assert the warning at `src/dotnet_ai_kit/copier.py:444`. The integration test avoids the skip path entirely by manufacturing every token key used in the repo (`tests/integration/test_init_token_resolution.py:16`, `tests/integration/test_init_token_resolution.py:22`, `tests/integration/test_init_token_resolution.py:34`).

3. **HIGH - the "end-to-end install" integration test is not gating anything meaningful.** It skips if the manifest is absent (`tests/integration/test_end_to_end_install.py:36`, `tests/integration/test_end_to_end_install.py:38`) and accepts `upgrade.returncode == 1` as success (`tests/integration/test_end_to_end_install.py:57`, `tests/integration/test_end_to_end_install.py:59`). It also does not assert `/cost`, despite T091 requiring it (`specs/018-fix-token-burn/tasks.md:226`). This would not catch the current non-atomic CLI upgrade path.

4. **MEDIUM - CI "every PR" is only weakly tested and currently targets master only.** The workflow triggers pull requests only to `master` (`.github/workflows/ci.yml:6`, `.github/workflows/ci.yml:7`), while the test only checks that the string `static-unit` exists (`tests/test_ci_config.py:18`, `tests/test_ci_config.py:20`). FR-029 says static+unit must run in CI on every PR (`specs/018-fix-token-burn/spec.md:187`). If this repo's normal integration branch is `development`, PRs to development will not run this workflow.

5. **MEDIUM - traceability has at least one false row.** The matrix claims FR-013 is covered by `tests/test_agents.py` (`specs/018-fix-token-burn/traceability.md:26`), but `tests/test_agents.py` contains no `skills`, `frontmatter`, or `expertise` assertion. The implementation change in `AGENT_FRONTMATTER_MAP` is real (`src/dotnet_ai_kit/agents.py:71`), but the traceability row overstates automated coverage.

6. **MEDIUM - the `/dai.learn` smoke test does not prove selective topic loading.** T065-smoke asks for seven files and selective `/dai.plan`/`/dai.review` reads (`specs/018-fix-token-burn/tasks.md:191`), but the test only runs `/dai.learn` and checks produced filenames/constitution length (`tests/smoke/test_learn_split.py:30`, `tests/smoke/test_learn_split.py:39`, `tests/smoke/test_learn_split.py:43`). It never runs plan/review or inspects a transcript for selective reads.

7. **LOW - FR-036 is manual-only despite FR-028 requiring static checks.** Traceability maps FR-036 to "manual audit" (`specs/018-fix-token-burn/traceability.md:50`), but FR-028 lists FR-036 in the static-check set (`specs/018-fix-token-burn/spec.md:183`).

## Spec/scope concerns

- **T043a cannot stay `[~]` if this branch is shipping against the current spec.** The tasks file accurately says CLI upgrade does not route through `run_upgrade()` (`specs/018-fix-token-burn/tasks.md:136`), but the spec lists SC-013 as a hard release gate (`specs/018-fix-token-burn/spec.md:214`, `specs/018-fix-token-burn/spec.md:228`). Either wire the CLI through the transactional orchestrator now or explicitly amend/de-scope FR-031/SC-013 before merge.
- **`mcp-state.yml` is a reasonable engineering fix, but it is a spec amendment.** FR-019 and SC-014 still say `config.yml` (`specs/018-fix-token-burn/spec.md:170`, `specs/018-fix-token-burn/spec.md:229`) while implementation intentionally uses `mcp-state.yml` (`src/dotnet_ai_kit/cli.py:308`). The branch should not ship with spec and implementation disagreeing about persisted user state.
- **`/dai.learn` remains prompt-only.** `commands/learn.md` tells the model to emit seven files (`commands/learn.md:29`, `commands/learn.md:48`), and there is no `cli.py::learn` command. That may be acceptable for slash-command architecture, but FR-024 says `/dai.learn` MUST produce the files (`specs/018-fix-token-burn/spec.md:175`), so the smoke test is the only real enforcement. Since that smoke is `[~]` and incomplete, the current evidence is not enough.
- **Constitution v1.0.7 reads coherently overall.** It consistently states 16 rules, 4 universal, 12 path-scoped (`.specify/memory/constitution.md:7`, `.specify/memory/constitution.md:119`, `.specify/memory/constitution.md:157`) and skills loaded on demand (`.specify/memory/constitution.md:126`, `.specify/memory/constitution.md:160`). The only mild stale wording is "Claude Code (v1.0)" (`.specify/memory/constitution.md:71`) while the feature relies on Claude Code v2.1.85+ for full hook fidelity.

## Nothing else

I did not write `READY-IMPLEMENTATION` to `codex-ready.txt`.

I could not run `pytest`, PyYAML sampling, or `python -m dotnet_ai_kit init <tmp>` in this workspace because `python`, `python3`, and `py` are not available on PATH. The findings above are from static inspection of the working tree.
