# Command File Contract Changes: Tool-Wide Quality Hardening

**Branch**: `016-tool-quality-hardening` | **Date**: 2026-04-04

## Branch Safety Section (clarify.md, implement.md)

### Exact text to add (copied from specify.md L184-195)

```markdown
## Secondary Repo Branch Safety

When projecting feature briefs to linked secondary repos:
1. Read linked repos from `.dotnet-ai-kit/config.yml` repos section
2. For each linked repo with a local path:
   - Run `git -C {repo_path} rev-parse --abbrev-ref HEAD` to check current branch
   - If on main/master/develop: `git -C {repo_path} checkout -b chore/brief-{NNN}-{name}`
   - If `chore/brief-{NNN}-{name}` already exists: `git -C {repo_path} checkout chore/brief-{NNN}-{name}`
   - If working directory dirty (`git -C {repo_path} status --porcelain`): warn and skip
3. After writing the brief, stage and commit on the chore branch
4. NEVER commit directly to main, master, or develop branches
```

### Placement

- **clarify.md** (173 lines): Insert before the final section. New total: ~185 lines (under 200).
- **implement.md** (184 lines): Insert before Error Handling. New total: ~196 lines (under 200).

### Consistency check

After changes, all 5 commands with secondary repo commits:

| Command | Branch Safety | Lines |
|---------|:------------:|-------|
| specify.md | Yes (L184-195) | 200 |
| clarify.md | **Yes (new)** | ~185 |
| plan.md | Yes (L118-127) | 132 |
| tasks.md | Yes (L188-194) | 200 |
| implement.md | **Yes (new)** | ~196 |

---

## event-flow.md Generation (plan.md)

### Text to add in the microservice mode section

Insert after the existing plan generation steps, before the output summary:

```markdown
### Step N: Generate Event Flow (microservice mode)

For microservice mode projects with inter-service communication:
1. Generate `event-flow.md` in the feature directory
2. Document each inter-service event:
   - **Event name**: Domain event class name
   - **Producer**: Service that publishes the event
   - **Consumer(s)**: Service(s) that subscribe
   - **Payload schema**: Key fields in the event payload
3. Include event ordering and idempotency notes where relevant
```

Current line count: 132. After addition: ~145 (under 200).

---

## Duplicate --dry-run Fix (9 commands)

### Affected files

add-aggregate.md, add-crud.md, add-endpoint.md, add-entity.md, add-event.md, add-page.md, add-tests.md, docs.md, explain.md

### Change pattern

**Before** (flags table):
```markdown
| `--dry-run` | Display generated code in terminal without writing to disk |
| `--dry-run` | List files that would be created/modified, no writes |
```

**After**:
```markdown
| `--dry-run` | Display generated code in terminal without writing to disk |
| `--list` | List files that would be created/modified with descriptions, no code output |
```

Also update "Preview / Dry-Run Behavior" sections and examples to distinguish the two flags.

---

## specify.md Step Reordering

### Current order (broken)

```
Step 4: Generate Specification (L91)
Step 5: Generate Quality Checklist (L130)
Step 4b: Project Feature Briefs (L152)  ← WRONG POSITION
Step 6: Write Output (L165)
```

### Corrected order

```
Step 4: Generate Specification
Step 5: Project Feature Briefs (was 4b)
Step 6: Generate Quality Checklist (was 5)
Step 7: Write Output (was 6)
```

Move L152-164 block before L130. Renumber steps sequentially.

---

## do.md Artifact Ownership Clarification

### Current text (L51-52)

```markdown
2. For microservice mode: generate `service-map.md` and `event-flow.md`.
```

### Updated text

```markdown
2. For microservice mode:
   - `service-map.md` is generated by the **specify** step (Step 1)
   - `event-flow.md` is generated by the **plan** step (this step)
   - If `service-map.md` does not exist from specify, generate it here
```
