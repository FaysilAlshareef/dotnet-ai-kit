# Spec fixture (Cursor FAIL branch)

## Assumptions

- **A-005**: Cursor sub-agent spike fixture failed under Cursor's loader;
  full Cursor sub-agent generation is deferred to v1.1.

## Success Criteria

- **SC-008**: Cursor smoke fixture failed in CI; the spec is revised per
  cursor-fixture-decision.contract.md fail-path.

## Out of scope

- **OOS-005**: Full Cursor sub-agent generation deferred to v1.1 (spike failed).
