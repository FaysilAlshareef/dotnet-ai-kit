# Spec fixture (Cursor PENDING branch)

## Assumptions

- **A-005**: Cursor sub-agent spike fixture pending the A-005 outcome JSON;
  the fixture at `agents/dotnet-ai-architect.md` ships as verification
  artifact only.

## Success Criteria

- **SC-008**: Cursor smoke fixture pending CI execution; final SC-008
  evaluation deferred to first post-tag workflow_dispatch run.

## Out of scope

- **OOS-005**: Full Cursor sub-agent generation pending the A-005 outcome
  (fail-safe default until CI flips the outcome JSON).
