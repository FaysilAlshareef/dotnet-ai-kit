# Repository-wide Copilot Instructions

## Project identity

- **company**: {{ company }}
- **domain**: {{ domain }}
- **side**: {{ side }}
- **project_type**: {{ project_type }}
- **architecture_branch**: {{ architecture_branch }}
- **dotnet_version**: {{ dotnet_version }}

## Always-on conventions

{% for rule_name, rule_body in convention_rules.items() %}
### {{ rule_name }}

{{ rule_body }}

{% endfor %}

## Architecture profile

{{ architecture_profile_body }}

## Path-scoped guidance

See the following path-scoped instruction files (each loads only when you
are editing files within its declared `applyTo` glob):

{% for area, glob_list in path_scopes.items() %}
- `.github/instructions/{{ area }}.instructions.md` — applies to: {{ glob_list | join(', ') }}
{% endfor %}

## Per-agent quick reference

The following specialist agents are available under `.github/agents/`:

{% for agent_name, agent_desc in agents.items() %}
- **{{ agent_name }}**: {{ agent_desc }}
{% endfor %}
