{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/FaysilAlshareef/dotnet-ai-kit/specs/019-plugin-native-arch/contracts/claude-plugin.schema.json",
  "title": "Claude Code Plugin Manifest (dotnet-ai-kit)",
  "description": "Schema for .claude-plugin/plugin.json under the plugin-native architecture (feature 019). All field names match Claude Code's documented plugin manifest reference (https://code.claude.com/docs/en/plugins-reference). Capabilities not documented for Claude Code MUST NOT appear here (per spec FR-002).",
  "type": "object",
  "required": ["name", "version", "description", "agents", "skills", "commands", "hooks", "mcpServers", "lspServers"],
  "additionalProperties": false,
  "properties": {
    "name": { "type": "string", "const": "dotnet-ai-kit" },
    "version": { "type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+(-[A-Za-z0-9\\.\\-]+)?$" },
    "description": { "type": "string", "minLength": 1 },
    "agents": {
      "type": "array",
      "items": { "type": "string", "pattern": "^agents-claude/.*\\.md$" },
      "minItems": 1,
      "description": "Per spec FR-026: each logical specialist agent has a Claude-generated file under agents-claude/. The legacy AGENT_FRONTMATTER_MAP is deleted (architecture-phase R1)."
    },
    "skills": {
      "type": "array",
      "items": { "type": "string", "pattern": "^skills/.*/SKILL\\.md$" },
      "minItems": 1
    },
    "commands": {
      "type": "array",
      "items": { "type": "string", "pattern": "^commands/.*\\.md$" },
      "minItems": 1
    },
    "hooks": {
      "type": "object",
      "required": ["configFile"],
      "additionalProperties": false,
      "properties": {
        "configFile": { "type": "string", "const": "hooks/hooks.json" }
      }
    },
    "mcpServers": {
      "type": "object",
      "required": ["configFile"],
      "additionalProperties": false,
      "properties": {
        "configFile": { "type": "string", "const": ".mcp.json" }
      },
      "description": "After commit 12: csharp-ls removed; codebase-memory-mcp retained."
    },
    "outputStyles": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Unused in v1; reserved per the host's documented manifest reference."
    },
    "lspServers": {
      "description": "Per https://code.claude.com/docs/en/plugins-reference, `lspServers` is `string|array|object`. Recommended form is a scalar path to a `.lsp.json` file at the plugin root (e.g., `./.lsp.json`) which maps server names to configs with required `command` + `extensionToLanguage` fields.",
      "oneOf": [
        {
          "type": "string",
          "pattern": "^\\./.+\\.json$",
          "description": "Path to an external `.lsp.json` file."
        },
        {
          "type": "array",
          "items": { "type": "string", "pattern": "^\\./.+\\.json$" },
          "description": "Array of paths to multiple `.lsp.json` files."
        },
        {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "required": ["command", "extensionToLanguage"],
            "properties": {
              "command": { "type": "string" },
              "extensionToLanguage": { "type": "object" }
            }
          },
          "description": "Inline server configs; each entry MUST declare command + extensionToLanguage per the docs."
        }
      ]
    },
    "dependencies": {
      "type": "array",
      "items": {
        "oneOf": [
          { "type": "string" },
          {
            "type": "object",
            "required": ["name"],
            "properties": {
              "name": { "type": "string" },
              "version": { "type": "string" }
            }
          }
        ]
      },
      "description": "Per docs: other plugins this plugin requires (with optional semver). Round-2 review removed the orphan `csharp-lsp` entry: there is no marketplace plugin by that name; LSP config lives under `lspServers`, not `dependencies`."
    },
    "userConfig": { "type": "object", "additionalProperties": true },
    "channels": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Unused in v1."
    }
  }
}
